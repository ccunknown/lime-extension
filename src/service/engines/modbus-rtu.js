const EventEmitter = require('events').EventEmitter;
const SerialPort = require(`serialport`);
const ModbusRTU = require('modbus-serial');

class ModbusSerialEngine extends EventEmitter {
  constructor() {
    this.serialport = 
  }

  init() {

  }
}

module.exports 